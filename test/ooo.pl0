const a = 10;
var b, c;


procedure  p;
  const x := 10;
  if a <= 10 then
    begin
     c := b + a
    end;
  begin
    read(p);
    while b # 0 do
      begin
        call q;
        write (2 * d);
        read(b);
      end
  end.
